window.GOGO_CONFIG = {
  // Supabase public project values. Safe to expose in a frontend app when RLS is enabled.
  supabaseUrl: "",
  supabaseAnonKey: "",

  // Stripe publishable key is optional when using Payment Links.
  stripePublishableKey: "",

  // Add live Stripe Payment Links later.
  paymentLinks: {
    pro: "",
    studio: ""
  }
};
